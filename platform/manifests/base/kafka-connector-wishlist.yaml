# Wishlist outbox — 단일 candle DB의 wishlist 스키마 outbox_events → Kafka.
apiVersion: kafka.strimzi.io/v1beta2
kind: KafkaConnector
metadata:
  name: wishlist-outbox
  namespace: kafka
  labels:
    strimzi.io/cluster: debezium
spec:
  class: io.debezium.connector.postgresql.PostgresConnector
  tasksMax: 1
  config:
    database.hostname: ${secrets:kafka/debezium-db:host}
    database.port: 5432
    database.user: ${secrets:kafka/debezium-db:username}
    database.password: ${secrets:kafka/debezium-db:password}
    database.dbname: candle
    topic.prefix: candle.wishlist
    plugin.name: pgoutput
    slot.name: debezium_wishlist
    publication.name: dbz_pub_wishlist
    publication.autocreate.mode: disabled
    table.include.list: wishlist.outbox_events
    transforms: outbox
    transforms.outbox.type: io.debezium.transforms.outbox.EventRouter
    transforms.outbox.route.by.field: event_type
    transforms.outbox.route.topic.replacement: ${routedByValue}
    transforms.outbox.table.field.event.key: aggregate_id
    transforms.outbox.table.field.event.type: event_type
    transforms.outbox.table.field.event.timestamp: occurred_at
    value.converter: org.apache.kafka.connect.json.JsonConverter
    value.converter.schemas.enable: false
