apiVersion: batch/v1
kind: CronJob
metadata:
  name: {{ .Values.name }}
  namespace: {{ .Values.namespace }}
  labels:
    {{- include "candle-batch.labels" . | nindent 4 }}
spec:
  schedule: "{{ .Values.schedule }}"
  timeZone: "{{ .Values.timeZone }}"
  concurrencyPolicy: {{ .Values.concurrencyPolicy }}
  startingDeadlineSeconds: {{ .Values.startingDeadlineSeconds }}
  suspend: {{ .Values.suspend }}
  jobTemplate:
    spec:
      backoffLimit: {{ .Values.backoffLimit }}
      ttlSecondsAfterFinished: {{ .Values.ttlSecondsAfterFinished }}
      template:
        metadata:
          labels:
            {{- include "candle-batch.labels" . | nindent 12 }}
          annotations:
            # native sidecar(istiod ENABLE_NATIVE_SIDECARS)로 주입 → Job 완료 시 사이드카 자동 종료.
            # 메시 합류로 소유 서비스 gRPC 호출에 mTLS 유지.
            sidecar.istio.io/inject: "true"
        spec:
          restartPolicy: {{ .Values.restartPolicy }}
          {{- if .Values.serviceAccountName }}
          serviceAccountName: {{ .Values.serviceAccountName }}
          {{- end }}
          containers:
            - name: {{ .Values.name }}
              image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
              imagePullPolicy: {{ .Values.image.pullPolicy }}
              args:
                - "--spring.batch.job.name={{ .Values.jobName }}"
              {{- if .Values.dbSecret }}
              envFrom:
                - secretRef:
                    name: {{ .Values.name }}-db
              {{- end }}
              {{- with .Values.env }}
              env:
                {{- toYaml . | nindent 16 }}
              {{- end }}
              resources:
                {{- toYaml .Values.resources | nindent 16 }}
