# app-of-apps 루트 (prod 클러스터 ArgoCD에 1회 적용)
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: candle-root
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: candle
  source:
    repoURL: https://github.com/candle/candle-k8s.git
    targetRevision: main
    path: platform/applications
    directory:
      recurse: false
      include: "{istio.yaml,strimzi.yaml,observability.yaml,platform-config-prod.yaml,services-prod.yaml}"
  destination:
    server: https://kubernetes.default.svc
    namespace: argocd
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
